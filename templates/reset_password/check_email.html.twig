{% extends 'base.html.twig' %}

{% block title %}Email de Réinitialisation de Mot de Passe Envoyé{% endblock %}

{% block body %}
    <div class="container padding-300">
        <div class="row padding-200">
            <p>
                Si un compte correspondant à votre adresse e-mail existe, un e-mail a été envoyé contenant un lien que vous pouvez utiliser pour réinitialiser votre mot de passe.
                Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
            </p>
            <p>Si vous ne recevez pas d'e-mail, veuillez vérifier votre dossier de spam ou <a href="{{ path('app_forgot_password_request') }}">essayez à nouveau</a>.</p>
        </div>
    </div>

{% endblock %}
